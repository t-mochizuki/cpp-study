#include <stdio.h>
#include <iostream>

using namespace std;

#define REP(a, i, n) for (int i = a; i < n; ++i)

int main() {
    int X, Y;
    cin >> X >> Y;

    cout << X + Y / 2 << endl;

    return 0;
}
