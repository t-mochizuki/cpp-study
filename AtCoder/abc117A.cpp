#include <stdio.h>
#include <iostream>

using namespace std;

#define REP(a, i, n) for (int i = a; i < n; ++i)

int main() {
    int T, X;
    cin >> T >> X;

    cout << 1.0 * T / X << endl;

    return 0;
}
