#include <stdio.h>
#include <iostream>

using namespace std;

#define REP(a, i, n) for (int i = a; i < n; ++i)

int main() {
    int AB, BC, CA;
    cin >> AB >> BC >> CA;

    cout << AB * BC / 2 << endl;

    return 0;
}
